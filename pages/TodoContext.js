import { createContext } from "react";

export const TodoContext = createContext(null)

//----------------------------------------------------------------

// import { createContext } from "react";

// export const TodoContext = createContext(null)